# This script is use to create two ECS instance and bind some shared volumes

Before using, you should prepare your authorization information in advance, including `access_key`, `secret_key` and `project_id`.
